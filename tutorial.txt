#Setup
Change the endpoint and ports according to your choice 

Edit the blacklist.json. In the list, add words, symbols, and strings that you want to extract

try onloading all libraries and configure versions with relevant features and settings

Run the main.py with the Python shell as an active process. Please provide root access to the process, especially in a Linux-based environment 


Connect the server endpoint to the address of the main host

this code is a quick developed model it may contains breakthroughs as no direct verification for headers is available in code itself but rely on blacklist for it



#test

try crafting a http request with blacklisted items

Send the malicious request to the server on which our code is run

Verify results and add suggestions

you may edit code for personalized experience but don't screw the code at its none workable condition 
